// STUB de contrat — implémenté par l'agent scène 3D.
import type { VrmStage } from './types'

export function createVrmStage(_container: HTMLElement): VrmStage {
  return {
    async loadModel() {},
    setEmotion() {},
    setSpeaking() {},
    dispose() {},
  }
}
